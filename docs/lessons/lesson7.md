# Як змінювати значення змінної

## 🎯 Мета уроку

- Навчитися **змінювати значення змінної**
- Дізнатися різні способи зміни змінної
- Ознайомитися з коротким записом зміни змінної
- Навчитися правильно **називати змінні**

---

## 🧩 Теоретичні відомості

Змінна називається **змінною**, тому що її значення **можна змінювати**.  
Python дозволяє робити це кількома способами.

---

## 1️⃣ Зміна значення змінної

Подивимось на приклад:

```python
x = 20
x = x + 5
```

🧠 Це читається так:
**«Візьми старе значення `x` і додай 5»**

Після цього у змінній `x` буде число **25**.

---

## 2️⃣ Короткий запис зміни змінної

Python дозволяє писати коротше.

### Повний запис:

```python
x = x + 5
```

### Короткий запис:

```python
x += 5
```

📌 Обидва варіанти роблять **одне й те саме**.

---

### 🔹 Інші короткі записи

```python
x -= 3   # відняти 3
x *= 2   # помножити на 2
x /= 4   # поділити на 4
```

---

## 3️⃣ Приклад із черепашкою

```python
from turtle import *

step = 40
forward(step)

step += 20
forward(step)

step += 20
forward(step)

done()
```

🧠 Кожен рух стає **довшим**, бо ми змінюємо змінну `step`.

---

## 4️⃣ Іменування змінних

Ім’я змінної повинно **пояснювати, що в ній зберігається**.

### ✅ Добрі імена змінних

```python
length = 100
angle = 90
radius = 50
step = 30
```

### ❌ Погані імена змінних

```python
a = 100
x = 90
q = 50
```

📌 Чим зрозуміліше ім’я змінної — тим легше читати програму.

---

## 5️⃣ Міні-приклад 🧪

```python
from turtle import *

distance = 60
forward(distance)

distance += 30
left(90)
forward(distance)

done()
```

Спробуй змінити число **30** на інше та подивись,
як зміниться малюнок.

---

## 🎨 Підсумок уроку

## 🧠 Важливо запам’ятати

* Значення змінної можна змінювати
* `x = x + 5` і `x += 5` — роблять одне й те саме
* Короткий запис допомагає писати код швидше
* Ім’я змінної має бути зрозумілим
* Зміна змінної змінює поведінку черепашки

---

