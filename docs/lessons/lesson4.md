#  **Кольорові фігури**

## 🎯 Мета уроку
- Навчитися переміщувати черепашку **без малювання**.  
- Ознайомитися з командами **підняття та опускання олівця**.  
- Вивчити, як задавати **колір лінії та колір заливки**.  
- Навчитися зафарбовувати **замкнені фігури**.  
- Створювати прості кольорові малюнки з кількох фігур.

---

## 🧩 Теоретичні відомості

Під час попередніх занять черепашка завжди рухалася і **залишала слід**.  
Але часто потрібно перемістити її в іншу точку **без малювання лінії** — наприклад, щоб намалювати кілька окремих фігур.

---

## 1️⃣ Піднімання та опускання олівця

Черепашка малює тільки тоді, коли її «олівець» опущений.  
Ми можемо керувати цим за допомогою двох команд.

### 🔹 Підняти олівець
```python
penup()
```
Черепашка може рухається, але не малює ліній.

### 🔹 Опустити олівець

```python
pendown()
```
Після цієї команди черепашка знову залишає слід.

---

### ✏️ Навіщо це потрібно?

* Перейти в нову точку перед початком малювання.
* Малювати кілька фігур окремо.
* Створювати складні малюнки без зайвих ліній.

> **Приклад:** Потрібно намалювати два квадрати поруч — один червоний, інший синій.
> Спочатку малюємо один, піднімаємо олівець, переходимо в інше місце, опускаємо олівець — малюємо другий.

---

## 2️⃣ Кольори ліній та заливки

Черепашка може малювати різними кольорами.
Для цього існує кілька команд.

## 🔹 Колір лінії (контуру)

```python
pencolor("blue")
```

Змінює колір того, що черепашка буде малювати.

---

## 3️⃣ Як зафарбувати фігуру

Щоб зафарбувати фігуру всередині, потрібно виконати три кроки.

### 🔹 1. Вибрати колір заливки

```python
fillcolor("yellow")
```

### 🔹 2. Почати заливку

```python
begin_fill()
```

### 🔹 3. Намалювати замкнену фігуру

(Наприклад квадрат, трикутник або коло)

### 🔹 4. Завершити заливку

```python
end_fill()
```

Після цього черепашка автоматично зафарбує внутрішню область.

---
### 🔹 Команда `color()` — два кольори одразу

У черепашки є зручна команда:
```python
color(колір_лінії, колір_заливки)
```

Вона дозволяє одним рядком встановити і колір контуру, і колір заливки.
```python
from turtle import *

pensize(10)
color("red", "yellow")
begin_fill()
forward(100)
left(90)
forward(100)
left(90)
forward(100)
left(90)
forward(100)
end_fill()

done()
```
<div style="display: flex; align-items: center;">
  <div style="flex: 1;">
    <ul>У цьому прикладі:
      <li> лінія буде червоною;
      <li> заливка — жовтою.
    </ul>
  </div>
  <div style="flex: 1; text-align: right;">
    <img src="../image_lessons/red_rect.png">
  </div>
</div>

---

## 🎨 Підсумок уроку 🧠 Важливо запам’ятати

* Черепашка може переміщатися **без малювання** за допомогою `penup()`.
* Щоб знову почати малювати, використовуємо `pendown()`.
* Колір лінії задається командою `pencolor()`.
* Колір заливки — `fillcolor()`.
* Або однією командою `color()` можна задати кольори
* Зафарбовування виконується командами
    -   `begin_fill()` 
    - → малювання фігури → 
    - `end_fill()`.
* Фігура повинна бути **замкнена**, інакше заливка прямуватиме до початкової точки.


