# Змінні та дії з числами

## 🎯 Мета уроку

- Дізнатися, що таке **змінна**
- Навчитися зберігати числа у змінних
- Використовувати змінні для керування черепашкою
- Виконувати прості дії з числами: додавання, віднімання, множення, ділення

---

## 🧩 Теоретичні відомості

До цього ми писали числа прямо в командах черепашки.  
А тепер навчимося **запам’ятовувати числа** та використовувати їх багато разів.

Для цього в Python існують **змінні**.

---

## 1️⃣ Що таке змінна?

**Змінна — це коробка, у якій зберігається число.**

```python
length = 100
```

* `length` — ім’я змінної
* `100` — число, яке ми зберегли

Тепер це число можна використовувати:

```python
forward(length)
```

---

## 2️⃣ Навіщо потрібні змінні?

Без змінної:

```python
forward(80)
left(90)
forward(80)
```

Із змінною:

```python
side = 80
forward(side)
left(90)
forward(side)
```

✔ менше помилок  
✔ легше змінювати числа  
✔ код стає зрозумілішим  

---

## 3️⃣ Дії з числами ➕➖✖️➗ 

Python уміє рахувати так само, як ми.

| Дія        | Приклад  |
| ---------- | -------- |
| Додавання  | `5 + 3`  |
| Віднімання | `10 - 4` |
| Множення   | `6 * 2`  |
| Ділення    | `8 / 2`  |

Приклад:

```python
length = 50 + 30
forward(length)
```

---

## 4️⃣ Використання змінних у черепашці

### 📏 Довжина руху

```python
distance = 120
forward(distance)
```

### 📐 Поворот

```python
angle = 90
left(angle)
```

### ⭕ Радіус кола

```python
radius = 60
circle(radius)
```

---

## 5️⃣ Міні-приклад 🧪

```python
from turtle import *

side = 100
angle = 90

forward(side)
left(angle)
forward(side)
left(angle)

done()
```

🧠 Якщо змінити **одне число**, зміниться весь малюнок.

---

## 🎨 Підсумок уроку

## 🧠 Важливо запам’ятати

* Змінна — це коробка для числа
* Змінні мають імена
* Числа у змінних можна використовувати багато разів
* Python уміє виконувати дії з числами
* Змінні допомагають керувати черепашкою

---

